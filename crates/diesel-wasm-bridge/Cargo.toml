[package]
name = "diesel-wasm-bridge"
edition = "2024"
version = "0.1.0"

[dependencies]
diesel = { version = "2.3.11", default-features = false, features = [
    "postgres_backend",
    "i-implement-a-third-party-backend-and-opt-into-breaking-changes",
] }
